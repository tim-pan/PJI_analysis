All CSV files record the performance of each model parameter setup in each dataset.

###############################################
#I only put the most important weight file(best parameter setup in best dataset) in this folder.
#Or it will looks like a mess. 
###############################################

c45_result_smote.csv records the performance of SM dataset.
c45_result_smote_pca.csv records the performance of SM+PCA dataset.
c45_result_rs.csv records the performance of RS dataset.
c45_result_rs_pca.csv records the performance of RS+PCA dataset.

draw_fig.ipynb visualize the outcome of each csv file.

Tree.dot is the decision rule of this classifier

Tree.png is the visualization of Tree.dot.

tr.xlsx and test_final.xlsx is the final training and testing datasets respectively.
(SM)


